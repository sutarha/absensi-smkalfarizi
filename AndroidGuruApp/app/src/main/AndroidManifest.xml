<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — SMK Al-Farizi Guru App (WebView Edition)
    Permission:
    - INTERNET: WebView load web
    - ACCESS_FINE_LOCATION + ACCESS_COARSE_LOCATION: GPS untuk presensi gerbang
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet & Network State untuk ConnectivityManager & WebView -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- GPS untuk jembatan presensi gerbang -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".GuruApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_home"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_home"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuruApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true">

        <!-- SplashActivity: Layar awal saat app dibuka -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.GuruApp.Splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity: WebView utama -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden" />

    </application>

</manifest>
